# aws-serverless-data-pipeline
Event-driven serverless data pipeline on AWS. Containerized Lambda (Docker) processes S3 uploads, performs data normalization, schema validation, and idempotent hashing, and separates valid and invalid records into structured storage paths.
